/*
 * *REUSABLE VERSION* displays accounting overlay popups TONIO Sept 8, 2010
 */
function loadAccountingPopupOverlay(controllerLink, title, func, marginLeft,
		top, contentMargin) {
	/*
	 * showOverlayContent(contextPath+controllerLink, title, func, marginLeft,
	 * top, contentMargin);
	 */
	showOverlayContent2(contextPath + controllerLink, title, 550, func, 90002);
}