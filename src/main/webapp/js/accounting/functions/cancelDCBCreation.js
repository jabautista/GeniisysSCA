function cancelDCBCreation() {
	customShowMessageBox("Cannot create an O.R without a DCB No.",
			imgMessage.INFO, "orTime");
	// showMessageBox("Message", "Cannot create an O.R without a DCB No.",
	// imgMessage.INFO);
}