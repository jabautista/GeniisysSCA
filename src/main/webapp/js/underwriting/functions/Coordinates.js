/**
 * Creates a coordinate Object.
 * @param row/x
 * @param column/y
 * @returns {Coordinates}
 */
function Coordinates(row, column) {
    this.row = row;
    this.x = row;
    this.column = column;
    this.y = column;
}